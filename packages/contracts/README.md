# @beacon/contracts

**OpenAPI-first** API contracts. `openapi.yaml` is the source of truth; Orval generates Zod
schemas + TanStack Query hooks. Generated code is never hand-edited.

**Lands here:** lifted from LienGuard's `lib/api-spec` (openapi.yaml + orval config),
`lib/api-zod` (generated Zod), and `lib/api-client-react` (generated hooks + custom-fetch).
Placeholder until the lift.
